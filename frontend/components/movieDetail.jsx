var React = require('react');
var MovieStore = require('../stores/movieList');
var ApiUtil = require('../util/apiUtil');
var MoviesIndex = require('./moviesIndex');

MovieDetail = React.createClass({
	
});

module.exports = MovieDetail;